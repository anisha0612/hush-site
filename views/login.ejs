<!DOCTYPE html>
<html lang="en">
  <%- include('partials/header.ejs'); -%>
  <body>
    <section class="container box pt-6">
      <div class="columns">
        <div class="column">
          <h1 class="title has-text-centered mt-6">Login</h1>
          <!-- < if(error!='') %>
          <div class="notification is-danger is-light">
            <button
              class="delete"
              onclick="this.parentElement.style.display='none'"
            ></button>
            <p><error%></p>
          </div>
          < %> < if(typeof errors!=="undefined")%> <for(let error of
          errors )%>
          <div class="notification is-danger is-light">
            <button
              class="delete"
              onclick="this.parentElement.style.display='none'"
            ></button>
            <p><error.msg%></p>
          </div>
          <><> -->
          <%- include('partials/messages.ejs'); -%>

          <form action="/login" method="POST">
            <div class="field">
              <label class="label">Email</label>
              <div class="control has-icons-left">
                <input
                  class="input"
                  type="email"
                  name="email"
                  placeholder="example@gmail.com"
                  required
                />
                <span class="icon is-small is-left">
                  <i class="fa fa-envelope"></i
                ></span>
              </div>
            </div>
            <div class="field">
              <label class="label">Password</label>
              <div class="control has-icons-left">
                <input
                  class="input"
                  type="password"
                  name="password"
                  placeholder="*******"
                  required
                />
                <span class="icon is-small is-left">
                  <i class="fa fa-lock"></i
                ></span>
              </div>
            </div>
            <div class="field has-text-centered mt-6">
              <button class="button" name="login" type="submit" value="login">
                Login
              </button>
            </div>
          </form>
        </div>
      </div>
    </section>
    <%- include('partials/footer.ejs'); -%>
  </body>
</html>
